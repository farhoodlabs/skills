apiVersion: v1
kind: Service
metadata:
  name: "{{SESSION_NAME}}"
  namespace: "{{NAMESPACE}}"
  labels:
    app: playwright-mcp
    session: "{{SESSION_NAME}}"
spec:
  type: ClusterIP
  selector:
    session: "{{SESSION_NAME}}"
  ports:
    - port: 8931
      targetPort: 8931
      protocol: TCP
